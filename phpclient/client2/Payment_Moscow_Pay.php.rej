--- Payment_Moscow_Pay.php	(revision 42942)
+++ Payment_Moscow_Pay.php	(revision 43111)
@@ -29,9 +29,9 @@
         $timezone = new DateTimeZone(date_default_timezone_get());
         $date = new DateTime('now', $timezone);
         $offset = $timezone->getOffset($date) / 3600;
-        if ($offset > 0) {
-            $offset = '+'.$offset;
-        }
+//         if ($offset > 0) {
+//             $offset = '+'.$offset;
+//         }
         return $offset;
     }
     private function mac($data) {
