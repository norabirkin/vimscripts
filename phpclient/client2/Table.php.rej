--- Table.php	(revision 42942)
+++ Table.php	(revision 43111)
@@ -16,7 +16,6 @@
     private $csv = false;
     private $displayPaging = true;
     private $hideOnEmpty = false;
-    private $fixSecondColumn = false;
     public function __construct($params) {
         $this->config($params);
     }
@@ -50,9 +49,6 @@
     public function setPagingTotal($value) {
         $this->pagingTotal = (int) $value;
     }
-    public function setFixSecondColumn($value) {
-        $this->fixSecondColumn = (bool) $value;
-    }
     public function setPagingPgnum($value) {
         $this->pagingPgnum = (int) $value;
     }
@@ -199,7 +195,16 @@
     }
     public function getColumns() {
         $columns = array();
-        foreach( $this->columns as $column ) $columns[] = $this->localize( $column );
+        foreach ($this->columns as $column) {
+            if (is_string($column)) {
+                $column = array(
+                    'title' => $column,
+                    'width' => null
+                );
+            }
+            $column['title'] = $this->localize($column['title']);
+            $columns[] = $column;
+        }
         return $columns;
     }
     private function getHeaders() {
@@ -219,7 +224,6 @@
             if ($this->rowClass) $class .= " " . $this->rowClass;
             $row = yii::app()->controller->renderPartial( "application.components.table.row", array(
                 "columns" => $this->getRow( $item ),
-                "fixSecondColumn" => $this->fixSecondColumn,
                 "class" => $class
             ), true );
             $rows .= $row;
